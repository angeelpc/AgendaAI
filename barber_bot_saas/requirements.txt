fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.6
httpx>=0.27
python-dotenv>=1.0
pytest>=8.0
# PostgreSQL (produccion en la nube). En local con SQLite no se usa.
psycopg2-binary>=2.9
# Opcional: solo si se usa el cerebro con IA real
anthropic>=0.34
